{
  "name": "starling",
  "version": "1.0.0",
  "description": "🐦 A smaller starline",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "esbuild src/main.js --outdir=www/dist --bundle --define:DEV=true --servedir=www --serve --watch",
    "build": "esbuild src/main.js --outdir=www/dist --bundle --define:DEV=false --minify --sourcemap"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14",
    "@types/three": "^0.164.0",
    "esbuild": "^0.21.3",
    "three": "^0.164.1"
  }
}
